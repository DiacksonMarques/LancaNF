import { ConsultObjectPipe } from './consult-object.pipe';

describe('ConsultObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
